# Repository task router

Before modifying repository files, read `.agents/change-work.md`.

Before committing, opening, reviewing, or merging a pull request—or publishing
or installing a release—also read `.agents/release-delivery.md`.

For read-only tasks, do not read or mention either deferred instruction file.
